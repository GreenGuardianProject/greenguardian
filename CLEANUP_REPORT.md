# Cleanup report

Record of the cleanup carried out on 2026-09-11 to turn this folder into a
public-ready repository. Nothing was pushed and no remote was created,
changed or contacted.

## Before you publish

1. **Remove or replace the `origin` remote.** It still points at the
   organisers' repository,
   `https://github.com/GreenDIGIT-project/greendigit-ecml-pkdd-2026-challenge.git`,
   and local `master` is 7 commits behind `origin/master`. I did not touch
   it.
2. **Confirm that `paper/GreenGuardian_Paper.pdf` is the camera-ready
   version.** I could not render it. Its first-page text gives the title and
   both authors, and contains none of "draft", "anonymous" or "arXiv". The
   PDF metadata says pdfTeX, created 2026-08-04. That is not proof it is
   final.
3. **Confirm with the organisers before publishing.** The repository carries
   their starter kit and their dataset.
4. **The citation in `README.md` has no proceedings details** (volume, pages,
   DOI). Add them when you have them.
5. **Local paths: resolved.** The history rewrite of 2026-09-11 (see "History
   rewrite" at the end of this report) replaced the absolute local paths in
   the three published `score_metrics.json` files with repository-relative
   paths, and redacted local paths and the personal e-mail address from this
   report, in every commit of `public-release`.
6. **Merge `public-release` into `master` yourself** after reading the diff.

## Git state

The commit SHAs in this section are from before the history rewrite. The
"History rewrite" section at the end maps them to the current ones.

- `master`: untouched at `da6c6c9` ("More info about README.md").
- `pre-cleanup-snapshot`: `85d594f`, a commit of the full working tree as it
  stood before any change: every non-ignored file, including all untracked
  results. It was made through a temporary index, so `master` and the
  working tree were not touched.
- `public-release`: the cleaned repository, one commit per step:

| Commit | Content |
|---|---|
| `d06b4e8` | Add GreenGuardian code as submitted (our three files, the scheduler `__init__.py` and our `run_simulation.py` changes, all unmodified) |
| `6c53460` | Clean up: remove egg-info, move paper and experiment, extend `.gitignore` |
| `fc9224c` | Fixes: package import, debug prints, `Iterable` import, dependency declarations, lock file |
| `0973ef9` | Results: submitted Task A outputs and published Task B score files |
| (last) | Documentation: READMEs, `REPRODUCE.md`, `LICENSE`, this report |

No git identity is configured on this machine. I did not change your git
config; every commit sets author and committer through environment variables
to Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com>.

## Backup

`%USERPROFILE%\greendigit-backup-20260911.tar.gz`, 233,766,628 bytes
(223 MB). It is a gzip-compressed tar of the entire folder as it stood before
any deletion, including `.git`, all three virtual environments and all
results. `gzip -t` passes, and `tar -tzf` lists 23,647 files, the same count
as `find` gave for the folder. Restore with
`tar -xzf %USERPROFILE%\greendigit-backup-20260911.tar.gz` from
`<the directory containing the repository>`.

## Disk size

| | Size |
|---|---|
| Before | 778 MB: virtual environments 683 MB (87.8%), generated output about 78 MB (10%), caches under 1 MB, everything else about 17 MB |
| After, excluding `.venv/` and `runs/` | 30 MB. `.git/` is 7.2 MB, up from 3.8 MB because of the snapshot branch's objects |
| New `.venv/` built for verification (git-ignored) | 332 MB |
| `runs/`, the verification outputs (git-ignored) | 92 MB |

`.venv/`, `runs/` and the two `*.egg-info/` directories, which the editable
install recreated, are the only ignored items left. The caches regenerated by
the verification runs were deleted again afterwards. Delete `.venv/` and
`runs/` whenever you like; they are not part of the release.

## Files and directories removed

| Path | Size | Why |
|---|---|---|
| `.venv_run/` | 374 MB, 12,917 files | Python 3.14 environment that produced the published numbers. Deleted only after `requirements-lock.txt` (976 bytes, 36 pins) was written from its `pip freeze` and confirmed on disk, and after the D2 test finished using it. |
| `task-a/.venv/` | 242 MB, 6,680 files | Dead: pointed at a Python 3.12 interpreter on a machine that no longer exists (problem 2). |
| `task-b/.venv/` | 67 MB, 3,691 files | Dead, same reason. |
| `task-a/model_6/__pycache__/`, `task-a/src/task_a/__pycache__/`, `task-a/src/task_a/models/__pycache__/`, `task-b/dirac_sim/__pycache__/`, `task-b/dirac_sim/api/__pycache__/`, `task-b/dirac_sim/baselines/__pycache__/`, `task-b/dirac_sim/core/__pycache__/`, `task-b/dirac_sim/schedulers/__pycache__/`, `task-b/examples/__pycache__/`, `task-b/tests/__pycache__/`, `task-b/.pytest_cache/` | under 1 MB total | Caches. No `.DS_Store` or stray `*.pyc` files existed outside these. |
| `task-a/src/task_a.egg-info/` (6 files) | 10 KB | Build artefact, tracked by the organisers, rewritten by `pip install -e` (D11). |
| `task-b/dirac_sim.egg-info/` (6 files) | 20 KB | Same (D11). |
| `task-b/debug_run.log` | 77,000 bytes | UTF-16 PowerShell log from the other machine, containing a `UnicodeEncodeError` trace. |
| `task-b/out.log` | 80,838 bytes | Same kind of log. |
| `task-a/src/task_a/models/ensemble_model.py` | 23,664 bytes | Earlier Task A experiment (LightGBM + Ridge); not used by the submission (D6). |
| `task-a/src/task_a/models/ensemble_model_v3.py` | 44,388 bytes | Earlier Task A experiment (LightGBM + LSTM); not used (D6). |
| `task-a/src/task_a/models/my_submissions/` | empty directory | Empty. |
| `task-a/model_6/outputs/metrics_cli.json` | 477 bytes | Stale: ScoreTA 0.7269 from an older run. The organisers' `task-a evaluate` on the current outputs gives 0.6439136560442531, the same as `metrics_model_6.json` (D4). |
| `task-b/results_live_clean/` (6 files) | 221 KB | One-day live-mode experiment, score 0.4997 (D5). |
| `task-b/results_live_ours_1day/` (6 files) | 205 KB | One-day live-mode experiment, score 0.5852 (D5). |
| `task-b/results_live_smoke/` (6 files) | 197 KB | Live-mode run that scored 0.0: the missing-`requests` failure (D5). |
| `task-b/results_model_6_v4_full/` (6 files) | 18 MB | Duplicate of `canonical`: identical CSVs, run on the other machine, and misnamed (it is the truncated window) (D5). |
| `task-b/results_model_6_v4_canonical/` | 18 MB | Truncated-window run. `score_metrics.json`, `score_summary.txt` and `dispatch_log.csv` kept (see Moved); `execution_report.csv` and both baseline execution reports dropped (D5). |
| `task-b/results_model_6_v4_papertail/` | 18 MB | Full-window (published) run. Same three files kept, execution reports dropped (D5). |
| `task-b/results_model_6_v4_normalized/` | 18 MB | Normalised-ranking experiment. `score_metrics.json` kept, the rest dropped (D6). |
| `README_SUBMISSION.md` | 4 KB | Submission note. Its commands were folded into `REPRODUCE.md`. It contained two e-mail addresses (D8). |
| `task-a/README_a.md` | 8 KB | The organisers' Task A README, renamed (identical apart from line endings). Merged into the new `task-a/README.md` (D9). |
| `task-b/README_b.md` | 8 KB | The organisers' Task B README, renamed (identical apart from line endings). Merged into the new `task-b/README.md` (D9). |
| `task-a/model_6/README.md` | 1 KB | Our short model note. Merged into `task-a/README.md` (D9). |

Every removed item is in the backup archive. Everything that was not
git-ignored is also in branch `pre-cleanup-snapshot`.

## Files moved

| From | To |
|---|---|
| `task-b/dirac_sim/schedulers/model_6_v4_normalized.py` | `experiments/model_6_v4_normalized.py` |
| `GreenGuardian_Paper.pdf` | `paper/GreenGuardian_Paper.pdf` |
| `figures_data/diurnal_profile.csv` | `paper/figures_data/diurnal_profile.csv` |
| `figures_data/intensity_distribution.csv` | `paper/figures_data/intensity_distribution.csv` |
| `task-b/results_model_6_v4_papertail/{score_metrics.json, score_summary.txt, dispatch_log.csv}` | `task-b/published_results/full_window/` |
| `task-b/results_model_6_v4_canonical/{score_metrics.json, score_summary.txt, dispatch_log.csv}` | `task-b/published_results/truncated_window/` |
| `task-b/results_model_6_v4_normalized/score_metrics.json` | `task-b/published_results/normalized_ranking/` |

The results files were copied, checked identical with `cmp`, and then the
source directories were deleted. The dispatch logs are 2,063,742 and
2,062,302 bytes, under the 5 MB threshold, so they were not gzipped.

## Files created

- `README.md`: rewritten; replaces the organisers' challenge README.
- `task-a/README.md`: recreated; replaces the organisers' Task A README.
- `task-b/README.md`: recreated; replaces the organisers' Task B README.
- `REPRODUCE.md`
- `LICENSE`: MIT, both authors.
- `CLEANUP_REPORT.md`: this file.
- `requirements-lock.txt`
- `experiments/README.md`
- `task-b/published_results/` and its 7 files (listed under Moved).
- Not committed, and git-ignored: `.venv/` (the fresh verification
  environment) and `runs/` (verification outputs).

## Files edited

| File | Change |
|---|---|
| `task-b/dirac_sim/schedulers/__init__.py` | Removed the imports of the nonexistent `model_6_green_window_v2`, `model_6_green_window_v3` and `model_6_v5`, and a commented-out import; exports only `Model6GreenWindowSchedulerV4` (problem 1). |
| `task-b/dirac_sim/schedulers/model_6_v4.py` | The two `print("DEBUG: ...")` calls become `logger.debug(...)`; added `import logging` and a module logger (problem 5). |
| `task-a/model_6/lgbm_core.py` | Added `from typing import Iterable` (problem 6). |
| `task-a/pyproject.toml` | New optional extra `model = ["lightgbm>=4.6", "numpy>=1.24", "pandas>=2.0"]` (problem 3). |
| `.gitignore` | Added `.venv*/`, `*.egg-info/`, `build/`, `dist/`, `.DS_Store`, `*.log`, `/runs/`, `/task-b/results_*/`, and `/task-a/model_6/outputs/*` with exceptions for the four submitted files. |
| `task-b/dirac_sim/core/wms.py` | Restored to the organisers' committed version. See the D2 section. |

## The seven known problems

| # | Problem | Resolution |
|---|---|---|
| 1 | `schedulers/__init__.py` imports three nonexistent modules | Fixed. `run_simulation.py` now imports and runs the scheduler from a clean install. |
| 2 | Dead `task-a/.venv/` | Removed, together with the equally dead `task-b/.venv/`. |
| 3 | `lightgbm` undeclared | Declared, with `numpy` and `pandas`, in the new `model` extra of `task-a/pyproject.toml`. |
| 4 | `requests` undeclared for live mode | It was already declared (`task-b/pyproject.toml` and `task-b/requirements.txt`). The failure happens when `dirac_sim` runs from an environment where it was not installed from its own pyproject. The documented install path now always does `pip install -e "./task-b[dev]"`. `task-b/README.md` states the failure plainly and gives the per-tick WARNING as the symptom. The file is `task-b/dirac_sim/api/forecast_client.py` (not `core/`); the empty-bundle return is at lines 97-99. |
| 5 | DEBUG prints in `model_6_v4.py` | Converted to `logger.debug`. |
| 6 | `Iterable` not imported in `lgbm_core.py` | Import added. |
| 7 | Leftover experimental files and result directories | Inventoried and handled per D4 to D7 (see Removed and Moved). |

## D2: the local modification to `wms.py`

The working copy of the protected `task-b/dirac_sim/core/wms.py` differed from
the organisers' committed version: a console progress bar, and three
`job_map` rebuilds replaced by direct `self.queue._jobs` lookups. As a test I
restored the organisers' version in a scratch copy of `task-b/`, with
`data/` and our `forecast_submission.csv`. I then ran both windows in the
environment that produced the published numbers (`.venv_run`, Python 3.14.3):

```bash
python examples/run_simulation.py --offline --objective carbon --start 2025-11-19T23:00:00 --end 2026-03-12T17:00:00 --scheduler dirac_sim.schedulers.Model6GreenWindowSchedulerV4 --output-dir <scratch>/out_truncated
python examples/run_simulation.py --offline --objective carbon --start 2025-11-19T23:00:00 --end 2026-03-13T17:00:00 --scheduler dirac_sim.schedulers.Model6GreenWindowSchedulerV4 --output-dir <scratch>/out_full
```

| | Expected | Pristine `wms.py` |
|---|---|---|
| Truncated `jobs_dispatched` | 28642 | 28642 |
| Truncated `energy_wh_total` | 771810.49064897 | 771810.49064897 |
| Truncated `cfp_g_total` | 310547.75864442 | 310547.75864442 |
| Truncated `final` | 0.7332082197926377 | 0.7332082197926377 |
| Full `jobs_dispatched` | 28662 | 28662 |
| Full `energy_wh_total` | 791810.49064897 | 791810.49064897 |
| Full `cfp_g_total` | 314547.75864442 | 314547.75864442 |
| Full `deadline_penalty` | 0.0 | 0.0 |
| Full `final` | 0.7317904319184824 | 0.7317904319184824 |

Both `dispatch_log.csv` files were also byte-identical to the published ones.
Each run took about 8.5 minutes. Everything matched exactly, so the release
carries the organisers' pristine `wms.py`, restored with
`git checkout HEAD -- task-b/dirac_sim/core/wms.py`. This is the full diff of
the local modification that was discarded:

```diff
diff --git a/task-b/dirac_sim/core/wms.py b/task-b/dirac_sim/core/wms.py
index 4526e12..0d85e3e 100644
--- a/task-b/dirac_sim/core/wms.py
+++ b/task-b/dirac_sim/core/wms.py
@@ -138,15 +138,32 @@ class WMSSimulator:
     # ------------------------------------------------------------------ #
     def run(self) -> "SimulationReport":
         """Run the simulation and return a report."""
+        import sys
         logger.info("WMSSimulator starting: %s -> %s",
                     self.start_time.isoformat(), self.end_time.isoformat())
 
         tick = self.start_time
         tick_delta = timedelta(minutes=self.TICK_MINUTES)
 
+        total_ticks = int((self.end_time - self.start_time) / tick_delta) + 1
+        current_tick = 0
+        scheduler_name = self.scheduler.__class__.__name__
+
+        sys.stdout.write(f"\r{scheduler_name} Progress: [                    ] 0%")
+        sys.stdout.flush()
+
         while tick <= self.end_time:
             self._tick(tick)
             tick += tick_delta
+            current_tick += 1
+            percent = min(100, int((current_tick / total_ticks) * 100))
+            bar_length = percent // 5
+            bar = "#" * bar_length + " " * (20 - bar_length)
+            sys.stdout.write(f"\r{scheduler_name} Progress: [{bar}] {percent}%")
+            sys.stdout.flush()
+
+        sys.stdout.write("\n")
+        sys.stdout.flush()
 
         self._complete_jobs_due(self.end_time + tick_delta)
 
@@ -218,9 +235,7 @@ class WMSSimulator:
 
     def _apply_plan(self, plan: DispatchPlan, now: datetime) -> None:
         for decision in plan.decisions:
-            job = self.queue.all_jobs()
-            job_map = {j.job_id: j for j in job}
-            j = job_map.get(decision.job_id)
+            j = self.queue._jobs.get(decision.job_id)
             if j is None or j.status != JobStatus.PENDING:
                 continue
 
@@ -238,8 +253,7 @@ class WMSSimulator:
         due = [d for d in self._pending_dispatches.values()
                if d.dispatch_at <= now]
         for decision in due:
-            job_map = {j.job_id: j for j in self.queue.all_jobs()}
-            j = job_map.get(decision.job_id)
+            j = self.queue._jobs.get(decision.job_id)
             if j and j.status == JobStatus.HELD:
                 dispatched = self._dispatch(j, decision, now)
                 if not dispatched:
@@ -309,9 +323,8 @@ class WMSSimulator:
         ]
         if not completed:
             return
-        job_map = {j.job_id: j for j in self.queue.all_jobs()}
         for record in completed:
-            job = job_map.get(record.job_id)
+            job = self.queue._jobs.get(record.job_id)
             site = self.registry.get(record.site_id)
             if job is not None:
                 self.queue.update_status(job.job_id, JobStatus.DONE)
```

## Verification (Phase 4)

I built a fresh environment by following the READMEs, with one deviation. On
this machine `python` resolves to the Microsoft Store alias, so the venv was
created with `py -3.14 -m venv .venv`. The root README carries this as its
Windows note. After activation, every command used `python` exactly as
written. All commands ran in Git Bash from the repository root, with
`source .venv/Scripts/activate`.

### Install

```bash
py -3.14 -m venv .venv
source .venv/Scripts/activate
python --version                                   # Python 3.14.3
python -m pip install --upgrade pip                # pip-26.2.1
python -m pip install -r requirements-lock.txt     # all 36 pins installed
python -m pip install -e "./task-a[model,dev]" -e "./task-b[dev]"
python -m pip check                                # No broken requirements found.
```

pip resolved `task-a[model,dev]` and `task-b[dev]` together with no
conflict. The resulting `pip freeze` is exactly the lock file plus four
dev-extra packages: `coverage==7.16.0`, `httpcore==1.0.9`,
`httpx==0.28.1` and `pytest-cov==7.1.0`.

### Tests

| Command | Result |
|---|---|
| `(cd task-a && python -m pytest)` | 10 passed |
| `(cd task-b && python -m pytest)` | 41 passed, 1 warning: `StarletteDeprecationWarning` from `fastapi/testclient.py` (using `httpx` with `starlette.testclient` is deprecated), third-party |

### Task A, our model: `(cd task-a && python model_6/train_model_6.py)`, 53 s

| Metric | Expected | Produced |
|---|---|---|
| `ScoreTA` | 0.6439136560442531 | 0.6439136560442531 |
| `S_A_4` | 0.7183765582728562 | 0.7183765582728562 |
| `S_A_96` | 0.8408457882937036 | 0.8408457882937036 |
| `S_A1` | 0.2136796978360075 | 0.2136796978360075 |
| `S_A2` | 0.4408925338037073 | 0.4408925338037073 |

`git status task-a/model_6/outputs` was empty afterwards: all four
regenerated files are byte-identical to the committed ones. As a cross-check,
the organisers' `task-a evaluate` on the same three CSVs also gave
`ScoreTA = 0.6439136560442531`, with identical components.

### Task A, reference baseline (the five `task-a` commands in `REPRODUCE.md` section 4), 4 s

| Metric | Expected | Produced |
|---|---|---|
| `ScoreTA` | 0.7646450002485659 | 0.7646450002485659 |

### Task B, our scheduler, truncated window, 12 min 21 s

```bash
(cd task-b && python examples/run_simulation.py --offline --objective carbon \
  --start 2025-11-19T23:00:00 --end 2026-03-12T17:00:00 \
  --scheduler dirac_sim.schedulers.Model6GreenWindowSchedulerV4 \
  --output-dir ../runs/task-b-truncated)
```

| Field | Expected | Produced |
|---|---|---|
| `jobs_dispatched` | 28642 | 28642 |
| `energy_wh_total` | 771810.49064897 | 771810.49064897 |
| `cfp_g_total` | 310547.75864442 | 310547.75864442 |
| `final` | 0.7332082197926377 | 0.7332082197926377 |

`dispatch_log.csv` is byte-identical to
`task-b/published_results/truncated_window/dispatch_log.csv`. The log has no
tracebacks and no `DEBUG:` prints.

### Task B, our scheduler, full window (the published run), 12 min 20 s

```bash
(cd task-b && python examples/run_simulation.py --offline --objective carbon \
  --start 2025-11-19T23:00:00 --end 2026-03-13T17:00:00 \
  --scheduler dirac_sim.schedulers.Model6GreenWindowSchedulerV4 \
  --output-dir ../runs/task-b-full)
```

| Field | Expected | Produced |
|---|---|---|
| `jobs_dispatched` | 28662 | 28662 |
| `energy_wh_total` | 791810.49064897 | 791810.49064897 |
| `cfp_g_total` | 314547.75864442 | 314547.75864442 |
| `deadline_penalty` | 0.0 | 0.0 |
| `final` | 0.7317904319184824 | 0.7317904319184824 |

`dispatch_log.csv` is byte-identical to
`task-b/published_results/full_window/dispatch_log.csv`. No tracebacks, no
`DEBUG:` prints.

### Task B baselines, full window

```bash
(cd task-b && python examples/run_simulation.py --offline --objective carbon \
  --start 2025-11-19T23:00:00 --end 2026-03-13T17:00:00 \
  --scheduler fcfs --output-dir ../runs/task-b-fcfs)
(cd task-b && python examples/run_simulation.py --offline --objective carbon \
  --start 2025-11-19T23:00:00 --end 2026-03-13T17:00:00 \
  --scheduler greedy_carbon --output-dir ../runs/task-b-greedy)
```

| Field | FCFS (10 min 35 s) | Greedy-carbon (10 min 25 s) |
|---|---|---|
| Exit status | 0, no tracebacks | 0, no tracebacks |
| `jobs_dispatched` | 28662 | 28662 |
| `energy_wh_total` | 7279431.33108995 | 7315516.60570242 |
| `cfp_g_total` | 3127038.1921551498 | 3113136.21978814 |
| `final` | 0.0 | 0.0006668597333677781 |

Both run without error. The FCFS totals equal the FCFS reference totals
recorded in the published full-window `score_metrics.json`. Greedy-carbon
logs 386 "site full; job re-queued" warnings from the organisers' simulator,
which is its normal behaviour.

### Normalised-ranking experiment (the command in `experiments/README.md`), 12 min 21 s

| Field | Recorded (`published_results/normalized_ranking`) | Produced |
|---|---|---|
| `final` | 0.7330277384073633 | 0.7330277384073633 |
| `pareto` | 0.5982364344306661 | 0.5982364344306661 |
| `declaration` | 0.13512275328239823 | 0.13512275328239823 |
| `deadline_penalty` | 0.0003314493057009482 | 0.0003314493057009482 |

The five Task B runs ran concurrently, which is why they took 10 to 12
minutes; alone, a run takes about 8.5 minutes. After all runs, `git status`
showed only the documentation changes. No tracked file was modified by any
run.

### Result

Every expected number reproduced exactly. No README instruction needed
correcting during verification. The one wording change made afterwards
softened an unverified claim in `task-b/README.md` about
`python -m dirac_sim simulate` from "the results differ" to "can differ".

### Protected files

`git diff --stat da6c6c9 HEAD` and `git diff --stat da6c6c9` over every
protected path are both empty. The protected paths are the Task A evaluator,
submission, schemas and `eval.yaml`; the Task B evaluator, `wms.py`,
`job_queue.py`, `site_model.py` and `scheduler.py`; `data/raw_metrics/`, the
four data files and `prepare_data.py`; and both `tests/` directories. The
same check covered the baselines, the Task A CLI and the organisers'
committed outputs.

## What I chose not to touch, and why

- **Protected files:** never edited. `wms.py` was restored to the organisers'
  committed version under the D2 authorisation.
- **The organisers' committed example outputs** (`task-a/outputs/`,
  `task-b/output/`, `task-b/results/`): kept untouched (D10). The
  verification runs wrote to `runs/` so they would not overwrite them.
- **`data/task_a_series.csv`:** the organisers' tracked file; kept.
- **`task-b/dirac_sim/api/forecast_client.py`:** its silent empty-bundle
  fallback is documented, not changed. Changing it would change behaviour,
  and it is the organisers' code.
- **`task-b/examples/run_simulation.py`:** our changes kept (TASKB_EVAL_END,
  FCFS cache, ASCII console output, UTF-8 files). Reverting them would change
  results. `task-b/README.md` lists them.
- **`task-b/pyproject.toml` and `task-b/requirements.txt`:** unchanged;
  `requests` was already declared.
- **The `origin` remote and `master`:** unchanged, as instructed.
- **Git configuration:** unchanged. The commit identity was passed through
  environment variables only.
- **`requirements-lock.txt`** omits `scikit-learn`, `joblib` and
  `threadpoolctl`, which were in `.venv_run` for the deleted ensemble
  experiments. No remaining code imports them, and the Task A numbers
  reproduce exactly without them.

## History rewrite (2026-09-11)

Two things had to be removed from `public-release` before publication:
local filesystem paths that revealed a Windows username, and the personal
e-mail address on the commits. A redaction commit on top would have left both
readable in the earlier commits, so the five commits were rewritten instead.
Nothing had been pushed, so the rewrite affected no one else.

### Safety ref and backup

- The tag `public-release-preredact` points at the pre-rewrite tip,
  `d1c6b8e40985e9b6a982cecd35a61555c62b053a`. It is a lightweight, local tag.
- I re-checked the backup tarball before rewriting. It is present,
  233,766,628 bytes, and passes `gzip -t`; `tar -tzf` lists 23,647 files,
  and a file extracted from it reads correctly.

### Tool

`git filter-repo`, version `a40bce548d2c`, installed with pip into a separate
virtual environment outside the repository, so that `.venv/` still matches the
lock file. It made one pass that did both jobs:

```bash
git filter-repo --force --refs da6c6c9..public-release \
  --replace-text <rules file> --mailmap <mailmap file>
```

The rules file and the mailmap are outside the repository. Before running, a
pre-flight check confirmed that each rule matched only its intended places.
`--refs da6c6c9..public-release` limited the rewrite to our five commits:
- The organisers' commits (`e668d1c`, `da6c6c9`) were not rewritten.
- `master`, `pre-cleanup-snapshot` and the tag were not touched.
- Partial mode left the `origin` remote in place; it was not removed.

### What was replaced

The replacements are described here in redacted form. Quoting the removed
strings would put them back into the repository. The verbatim before/after
table is kept outside the repository and is not part of this report.

| Rule | Where (commits before the rewrite) | What was removed | Replacement |
|---|---|---|---|
| R1 | The three published `score_metrics.json` files, values `run.jobs`, `run.sites` and `run.forecast_csv`: 9 values in `0973ef9` and `d1c6b8e` | The absolute Windows path of the repository's `data` directory on the author's machine, including the username and the cloud-sync folder | `data/`, giving `data/job_trace.csv`, `data/site_config.json` and `data/forecast_baseline.csv` |
| R2 | `CLEANUP_REPORT.md`, checklist bullet 5 (`d1c6b8e`) | An example of that absolute path | `<local path>` |
| R3 | `CLEANUP_REPORT.md`, Backup section, 2 places (`d1c6b8e`) | The absolute path of the backup archive in the author's user profile | `%USERPROFILE%\greendigit-backup-20260911.tar.gz` |
| R4 | `CLEANUP_REPORT.md`, Backup section (`d1c6b8e`) | The absolute path of the directory containing the repository | `<the directory containing the repository>` |
| R5 | `CLEANUP_REPORT.md`, removed-files table, `task-a/.venv/` row (`d1c6b8e`) | The interpreter path of the dead environment, including another person's username | "pointed at a Python 3.12 interpreter on a machine that no longer exists" |
| R6 | `CLEANUP_REPORT.md`, Git state section (`d1c6b8e`) | The author's personal e-mail address | `48059346+NikosVat@users.noreply.github.com` |
| M | Author and committer of all five commits | The author's personal e-mail address | `Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com>`; the name and dates are unchanged |

These were left in place by decision:
- the organisers' server paths in `data/raw_metrics/summary_sites_metadata.json`
  and `task-b/results/score_metrics.json`, which are their content at their
  commits and already public upstream;
- the two `@uth.gr` addresses in the paper's author block.

`paper/GreenGuardian_Paper.pdf` was not altered: its blob is `ed7d0ada`
before and after, so its bytes are identical.

In this sixth commit, checklist bullet 5 was rewritten to say the local paths
are resolved, and a note was added under "Git state" that its SHAs predate
the rewrite.

### Commit mapping

| Before | After | Message (unchanged) |
|---|---|---|
| `d06b4e8699f621e959e78a88fc166ec34a58d9d6` | `3e0c434d2d0451b3a2e9687fc1c1fe89de976e3b` | Add GreenGuardian code as submitted |
| `6c53460911af4c0161ac5c891d619938325dc7d3` | `f4b803bf4896ee67d996a2c00d4e56e9e1ae0c2b` | Clean up repository for public release |
| `fc9224c5b7753e9a953570068d61d343e071bc21` | `b1f0fb825ee35ca9558fac48163eba222ac88620` | Fix import failure, debug output and undeclared dependencies |
| `0973ef95261eec5030a7fcbfcc41a7fb96c739cc` | `271ef7abb81fe588f1422aeaf0d4ad07a5cbc164` | Add submitted Task A outputs and published Task B results |
| `d1c6b8e40985e9b6a982cecd35a61555c62b053a` | `037ec3c6cf50248294243ee49d314719d0a8b935` | Add documentation, licence and cleanup report |

The parent of `3e0c434` is still `da6c6c98c65fa027ca0ec6e04d1a80280d4f6854`.
The first three commits changed SHA only because of the mailmap and the new
parent chain; their trees are unchanged.

### Verification

**Leak scan.** A scan of every blob in `e668d1c`, `da6c6c9`, `3e0c434`,
`f4b803b`, `b1f0fb8`, `271ef7a` and `037ec3c` (136 blobs) searched for the
username, the other person's username, the user-profile and sync-folder path
fragments, any drive-letter path, and the personal e-mail address. It found
none of them. The only matches from its wider pattern list are the items
left in place by decision: the organisers' server paths in the two files
above, the paper's author block, and coincidental byte sequences inside the
compressed PDF and PNG data. After this sixth commit exists, the same scan is
repeated across all six commits.

**Authors and committers**
(`git log --format='%h | A: %an <%ae> | C: %cn <%ce>' da6c6c9..public-release`,
before this sixth commit):

```text
037ec3c | A: Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com> | C: Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com>
271ef7a | A: Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com> | C: Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com>
b1f0fb8 | A: Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com> | C: Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com>
f4b803b | A: Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com> | C: Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com>
3e0c434 | A: Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com> | C: Nikolaos Vatopoulos <48059346+NikosVat@users.noreply.github.com>
```

The author and committer dates are identical to those before the rewrite for
every commit (2026-09-11 18:22:30, 18:27:07, 18:27:08, 18:27:08 and 18:55:13,
all +0300).

**Commit messages.**
`diff <(git log --format='%B%n---' da6c6c9..public-release-preredact) <(git log --format='%B%n---' da6c6c9..public-release)`
printed nothing: the messages are identical.

**Per-commit tree differences, before vs after** (`git diff --name-only`):

```text
d06b4e8 -> 3e0c434: (none)
6c53460 -> f4b803b: (none)
fc9224c -> b1f0fb8: (none)
0973ef9 -> 271ef7a: the three published score_metrics.json files
d1c6b8e -> 037ec3c: CLEANUP_REPORT.md and the three published score_metrics.json files
```

**Whole tree** (`git diff --stat public-release-preredact public-release`):

```text
 CLEANUP_REPORT.md                                            | 12 ++++++------
 task-b/published_results/full_window/score_metrics.json      |  6 +++---
 .../published_results/normalized_ranking/score_metrics.json  |  6 +++---
 task-b/published_results/truncated_window/score_metrics.json |  6 +++---
 4 files changed, 15 insertions(+), 15 deletions(-)
```

Every other tracked file is byte-identical to `public-release-preredact`.

**Our code as submitted.** The tree of `3e0c434` is identical to that of
`d06b4e8` (`36228a4c683e7b09fd8747b6f0acaf19acd9accf`). The three files we
wrote have the same blob IDs:

```text
task-a/model_6/train_model_6.py           fca755a67f16cbf1b35b4c7e8102559fca657420
task-a/model_6/lgbm_core.py               cb81e24c0f199c0adec3e8064e4a2040e7f6bc06
task-b/dirac_sim/schedulers/model_6_v4.py 52b693f247cc52179f7c6199943a94e4dc3abcec
```

**`score_metrics.json` files.** Each was parsed with `json.loads`, and the
bytes from `"evaluation"` to the end of the file were compared with the same
file at `public-release-preredact`:

```text
full_window: parses OK | evaluation block bytes identical: True (948 bytes) | evaluation dict equal: True | run keys changed: ['jobs', 'sites', 'forecast_csv'] | new run paths: data/job_trace.csv, data/site_config.json, data/forecast_baseline.csv | final: 0.7317904319184824
truncated_window: parses OK | evaluation block bytes identical: True (1003 bytes) | evaluation dict equal: True | run keys changed: ['jobs', 'sites', 'forecast_csv'] | new run paths: data/job_trace.csv, data/site_config.json, data/forecast_baseline.csv | final: 0.7332082197926377
normalized_ranking: parses OK | evaluation block bytes identical: True (1007 bytes) | evaluation dict equal: True | run keys changed: ['jobs', 'sites', 'forecast_csv'] | new run paths: data/job_trace.csv, data/site_config.json, data/forecast_baseline.csv | final: 0.7330277384073633
```

**Repository state.** `git status` was clean after the rewrite. `master` is
at `da6c6c98c65fa027ca0ec6e04d1a80280d4f6854`, `pre-cleanup-snapshot` at
`85d594f286ae0af24e4632e837ac4ed7848df930`, and the tag at
`d1c6b8e40985e9b6a982cecd35a61555c62b053a`.

**Task B reruns after the rewrite.** Both runs used fresh output directories
and the fresh `.venv/`, from `task-b/`:

```bash
python examples/run_simulation.py --offline --objective carbon \
  --start 2025-11-19T23:00:00 --end 2026-03-12T17:00:00 \
  --scheduler dirac_sim.schedulers.Model6GreenWindowSchedulerV4 \
  --output-dir ../runs/redact-truncated
python examples/run_simulation.py --offline --objective carbon \
  --start 2025-11-19T23:00:00 --end 2026-03-13T17:00:00 \
  --scheduler dirac_sim.schedulers.Model6GreenWindowSchedulerV4 \
  --output-dir ../runs/redact-full
```

| Field | Truncated, expected | Truncated, produced (7 min 17 s) | Full, expected | Full, produced (7 min 9 s) |
|---|---|---|---|---|
| `jobs_dispatched` | 28642 | 28642 | 28662 | 28662 |
| `energy_wh_total` | 771810.49064897 | 771810.49064897 | 791810.49064897 | 791810.49064897 |
| `cfp_g_total` | 310547.75864442 | 310547.75864442 | 314547.75864442 | 314547.75864442 |
| `deadline_penalty` | | 0.000348894006000966 | 0.0 | 0.0 |
| `final` | 0.7332082197926377 | 0.7332082197926377 | 0.7317904319184824 | 0.7317904319184824 |

Both `dispatch_log.csv` files are byte-identical to the checked-out
`task-b/published_results/*/dispatch_log.csv`. Compared with the committed
blobs, the only difference is line endings. Git stores the blobs with LF
(`core.autocrlf=true` on this machine), and the simulator writes CRLF on
Windows. With the CR bytes removed, the reruns match the committed blobs byte
for byte:

```text
core.autocrlf=true
truncated: CR bytes: committed blob=0 checked-out=28643 rerun=28643 | lines: 28643
           checked-out file vs rerun: byte-identical
           committed blob vs rerun with CR removed: byte-identical
full:      CR bytes: committed blob=0 checked-out=28663 rerun=28663 | lines: 28663
           checked-out file vs rerun: byte-identical
           committed blob vs rerun with CR removed: byte-identical
```

The rewrite changed only path strings, and the published numbers reproduce
exactly after it.

### Remaining pre-publish checklist

1. **Remove or replace the `origin` remote.** It still points at the
   organisers' repository; the rewrite did not remove it.
2. **Confirm that `paper/GreenGuardian_Paper.pdf` is the camera-ready
   version.** Its bytes are unchanged by the rewrite, so you can compare it
   directly.
3. **Confirm with the organisers** that they are happy for their starter kit
   and dataset to be republished.
4. **Add proceedings details** (volume, pages, DOI) to the citation in
   `README.md`.
5. **Push only the branch you intend to publish, by name.** Never push
   `pre-cleanup-snapshot` or the tag `public-release-preredact`, and never
   use `--all`, `--mirror` or `--tags`: both still contain the unredacted
   content. Delete them once you are satisfied. The backup tarball holds the
   same unredacted data and must stay private.
6. **Set the commit identity before any further commit.** No git identity is
   configured on this machine. Set `user.email` to the noreply address in
   this repository; a commit made with the personal address would bring it
   back.
7. **Merge `public-release` into `master` yourself** after reading the diff.
   `master` is its base, so the merge is a fast-forward.
8. **Delete the verbatim redaction table** outside the repository once you
   have read it.
